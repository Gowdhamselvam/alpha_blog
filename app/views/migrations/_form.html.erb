<%= form_with(model: migration, local: true) do |form| %>
  <% if migration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(migration.errors.count, "error") %> prohibited this migration from being saved:</h2>

      <ul>
      <% migration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :create_users %>
    <%= form.text_field :create_users %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
